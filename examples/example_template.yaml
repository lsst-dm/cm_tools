includeConfigs:
- ${{CTRL_BPS_DIR}}/config/bps_idf.yaml
- requestMemory.yaml
- clustering_alt.yaml

project: example
campaign: example/test
pipelineYaml: {pipeline_yaml}

payload:
  payloadName: example/test
  output: "{w_coll_out}"
  butlerConfig: {butler_config}
  inCollection: "{w_coll_in}"
  dataQuery: "{data_query}"
  sw_image: "{sw_image}

saveClusteredQgraph: True
saveGenericWorkflow: True